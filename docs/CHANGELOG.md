# 機能追加の取りまとめ

本プロジェクトで実装してきた機能の経緯をまとめたものです。
機能の使い方は [管理者向け機能ガイド](ADMIN-GUIDE.md)、設計の確定事項は [SPEC.md](SPEC.md) を参照してください。

## 初期リリース（2026-06-11）

ランダムマッチング型のビデオ通話Webアプリとして作成。

- ユーザー登録・ログイン（JWT認証）
- ダッシュボード（お知らせ・イベントカレンダー・みんなの掲示板・セッション履歴・オンライン統計）
- ランダムマッチング → 双方同意 → 1対1ビデオ通話（WebRTC）→ 振り返りアンケート
- GitHub連携・Google Cloud Runへの自動デプロイ（pushするだけで反映）

## ブランドリニューアル「対話のおけいこ」（2026-06-11）

企画書に基づき全面リブランド。

- 配色を紺×オレンジ×生成りに変更、文言を「聴く力」コンセプトに統一
- 10分セッションタイマー（時間が来ると自動終了）

## 役割マッチングとセッション呼び名（2026-06-12）

- 「話し手」「聞き手」を選んで待機し、**反対の役割とのみマッチング**
- ダッシュボードに役割別の待機人数を表示
- マッチごとにランダムな**セッション限定の呼び名**を割り振り（本名・ユーザー名は相手に伝えない）
- 通話画面に役割と呼び名を常時表示

## アバター（2026-06-12）

- **デフォルメモード**: MediaPipe顔トラッキング連動の2Dアバター5種（まる・ねこ・うさぎ・くま・ひと）。
  まばたき・口の開閉・笑顔・眉・頭の向き/うなずきに連動。カメラの実映像はネットワークに流れない
- **リアルモード**: 3D VRMアバター5種（ひなた・つむぎ・たける・れん・シード）。すべて再配布可能なライセンスを確認済み
- トラッキング修正: うなずきの上下張り付き（カメラ位置の自動キャリブレーション）、VRM0系モデルの回転反転、構図調整

## ユーザーロールと管理画面（2026-06-12）

- 管理者ロールを導入し、起動時に `administrator` を自動作成
- 管理画面: ユーザー一覧・他ユーザーのセッション履歴閲覧・ユーザー削除・サイト設定（セッション時間・新規登録の許可）・お知らせ管理

## マルチテナント化（2026-06-12）

仕様書（SPEC.md）に基づく4フェーズで実装。

- **フェーズ1: サイト分離** — サイトID単位でユーザー・マッチング・データを完全分離。
  ログイン2系統（メインサイト `/taiwa-lesson`・サブサイト `/login`）、
  4段階ロール（システム管理者＞サイト管理者＞モデレータ＞ユーザー）、
  初期パスワードの初回ログイン時変更強制、サイト管理者の自動生成、
  システム管理画面（サイト作成・削除・各サイトの確認）
- **フェーズ2: 通話と表示の設定** — 表示モード4種（デフォルメ/リアル/静止画/実映像）の利用可否、
  匿名/実名切替、役割なしマッチング、アンケートの有無・設問カスタマイズ
- **フェーズ3: チーム** — 複数所属可のチーム、チームリーダー（招待・削除）、
  チーム限定の掲示板・スケジュール、チーム統計
- **フェーズ4: ルーム** — ルームごとのマッチング（ルーム通話）。モデレータ以上が作成。
  ルーム設定（見える範囲・合言葉・定員・存在期間・ルーム管理者・通話設定の上書き）

## 検討枠5機能（2026-06-12）

- サイト別ブランディング（サイト名・キャッチコピー）
- ユーザーCSV一括登録（パスワード自動生成対応）
- 利用レポート（指標・日別グラフ・チーム別集計）
- 監査ログ（管理操作の記録）
- カレンダー→ルーム連携（予定からワンタップでルーム参加）

## トラスト&セーフティ・通話機能・体験向上（2026-06-12）

- **通報**（理由つき。サイト管理者とチームリーダーのみ閲覧。管理画面に通報キュー）
- **ブロック**（この相手と二度とマッチしない）
- **警告文**（サイト管理者・チームリーダーが発令。対象のログイン時に画面中央ポップアップ）
- **パスワードリセット**（サイト管理者・モデレータが新しい初期パスワードを発行）
- 通話機能: **ミュート・映像オフ・画面共有・セッション内チャット**（利用可否はサイト設定）。
  チャットログは終了画面で保存を促してから削除
- **役割交代つきセッション**（10分×2回。時間終了時に双方同意で役割を交代してもう1回）
- マッチ成立の通知音
- **話題カード**（ルーム設定／話し手が設定／ロビーはなし・ランダム・任意。独自アセット対応）
- **デバイステスト**（カメラ・マイクの選択、プレビュー、音量メーター、アバターのお試し）
- レポートCSVエクスポート、アンケート複数設問化、メール連携（SMTP設定時）、
  「また話したい」相互一致の再マッチ優先

## PWA化（2026-06-12）

- スマホのホーム画面に追加してアプリのように起動可能（manifest・アイコン・Service Worker）
- VRMモデルとアイコンのみキャッシュし、コード類は常に最新を取得

## 管理画面の再構成と運用機能（2026-06-12〜13）

- **管理画面をタブ式の4ページに分割**: ユーザー（横幅いっぱい）／サイト設定（5セクションのカードに整理：
  サイト全体・マッチング・通話・通話前・アンケート）／レポート・ログ／チーム・お知らせ
- **システム管理画面も3タブに分割**: サイト一覧（全幅・サイト名の視認性改善）／サイト作成／サイトの詳細
  （「ユーザー」「設定」クリックで自動遷移）
- **ユーザー管理の強化**: 各カラムのソート（▲▼）、ロールのプルダウン変更（一般／モデレータ／サイト管理者）、
  ユーザーの**有効化/無効化**（無効化中はログイン・既存トークン・WebSocketすべて拒否）、
  ユーザー一覧のCSVエクスポート、セッション履歴のモーダル表示
- **administratorの初回パスワード変更をデモ版では省略**（本番で復活させる旨をTODOに記録）
- **デモデータの自動生成・削除**（システム管理画面・本番では撤去）: デモユーザー12名・チーム3つ・
  通話履歴30回・掲示板・イベント・ルームに加え、**デモ用サブサイト「demo-corp（デモ株式会社）」**
  （ユーザー7名・履歴・実名/実映像の社内向け設定）をワンクリックで生成/削除

## ダッシュボード・モーダルのUI改善（2026-06-13）

- **ルームをカード表示でダッシュボード上部に配置**。カードに名前・話題・参加人数/定員・
  セッション時間・役割を表示
- **ルームカード押下で詳細モーダルを表示**（話題・参加状況・マッチング方式・表示モード・
  作成者・有効期限など）。モーダル内の「マッチング開始」で待機へ。役割マッチングが有効な
  ルームでは「話し手として参加」「聞き手として参加」の2ボタンを配置。合言葉はモーダル内で入力
- **イベント作成をモーダル化**（カレンダーは表示専用に）。「イベントを作成」ボタンまたは
  日付クリックでモーダルが開き、日付・イベント名・公開範囲・ルーム連携を入力
- **すべてのモーダルが背景クリックで閉じる**ように統一

## チーム画面とマイページ（2026-06-13）

- **チーム画面を新設** — ダッシュボードの「マイチーム」やマイページから「開く」で遷移。
  管理画面に入れない**チームリーダーの管理導線**になる
  - チーム情報（名前・**説明文**(新設)・統計）／メンバー一覧／チーム掲示板／今月のチーム予定／
    リーダー向けの通報一覧
  - リーダーはチーム設定の変更・メンバー招待・削除・警告ができる
- **チームリーダーの複数人化** — リーダーが共同リーダーを任命/解除できる
  （自分自身の権限変更は不可。サイト管理者は全操作可）
- **チームからの脱退**（セルフ削除）。最後のリーダーは他のリーダーを任命するまで脱退不可
- **マイページを新設** — 右上のユーザー名をクリックで表示。プロフィール
  （ユーザー名・権限・サイト・登録日・セッション回数）、メールアドレスの変更、
  パスワード変更、セッション履歴、所属チーム（チーム画面への導線）

## テーマ切替（2026-06-13）

- **5種類の配色テーマ** — スタンダード（紺×コーラル）・ダーク・さくら・わかば・ラベンダー。
  マイページの「テーマ」から色見本つきボタンで選択し、即時反映
- 選択は `localStorage`（`vm_theme`）に端末ごとに保存。CSS読込前に適用してちらつきを防止
- 実装はCSSカスタムプロパティの一括上書き（`<html data-theme="...">`）。
  PWAのステータスバー色（`theme-color`）もテーマに追従

## システム管理・ヘッダー・表示名の再設計（2026-06-13）

- **ヘッダー刷新**: 左上のサイト名クリックでホームへ戻る。右上はユーザーアイコン（👤表示名▾）の
  ドロップダウンメニュー（設定=マイページ／ログアウト）。**システム管理ボタンは金色の特別な
  見た目**にして、全サイト横断の特権操作であることを明示
- **システム管理画面の再構成**:
  - サイト一覧のCSVエクスポート。サイト名クリックで**サイト詳細ページ**へ遷移
  - サイト詳細は**「ユーザー一覧」「設定確認」の2タブ**。ユーザー一覧の各行に「編集」
    （表示名・ロール・有効/無効をモーダルで変更）。設定確認タブの「編集」でサイト設定と
    同じ全項目を編集可能（`PUT /api/sysadmin/sites/{id}/settings`）
  - 「サイト名を変更」でサイトの表示名を変更（サイトID=slugは紐付け用のため変更不可）
- **チーム管理のモーダル化**: 管理画面でチーム名クリック→詳細モーダル（内部ID・説明・統計・
  メンバー）→「編集」でチーム名・説明・リーダー任命/解除・メンバー追加/削除。
  編集ボタンは権限（リーダーまたはサイト管理者）がある場合のみ表示
- **表示名（display_name）の導入**: ログインID（username）と表示用の名前を分離。
  マイページ・管理画面・システム管理画面から変更可能。掲示板・イベント・チーム・通報・
  通話相手名・ルーム作成者などの表示はすべて表示名を使用（内部の紐付けは従来どおり整数IDのFK）
- 管理画面ユーザー一覧も「詳細→編集」パターンに統一（ロール変更・有効/無効は編集モーダルへ集約）

## 本日の予定・イベント時刻・出欠制マッチング（朝会）（2026-06-13）

- **イベントに開始時刻**（任意）を追加。カレンダー一覧・本日の予定に時刻つきで表示
- **本日の予定バナー**: ダッシュボードの「おかえりなさい」とルームの間に、当日の予定だけを
  表示（サイト全体 + 自分の所属チーム）。`/api/events/today?date=` で取得
- **参加可否（RSVP）**: 各イベントに「参加／不参加」をユーザーごとに登録（`EventAttendance`）。
  本日バナーから切り替え、参加予定人数も表示
- **イベント→ルーム入室**: ルーム連携イベントは本日バナー・カレンダーから「入室する」で
  ルーム入室モーダルを開ける
- **出欠制マッチング（朝会）**: ルームに「出欠制」フラグを追加。ONにすると、その日に
  そのルームへ紐づくイベントで「参加」を選んだ人だけが待機列に入れる
  （`Room.attendance_required`）。チーム（見える範囲）と本日のイベントを組み合わせると、
  **参加可能なチームメンバーだけをランダムにマッチング**できる
  - WebSocketの `join_queue` にクライアントのローカル日付を渡して当日判定。出欠未登録・
    不参加・本日イベントなしは入室をブロックし、理由を返す
- デモデータに「デモ_朝会ルーム」（営業チーム限定・出欠制）+ 本日の朝会イベント +
  メンバーのRSVPの例を追加

## 繰り返しイベント（Googleカレンダー風）（2026-06-13）

- イベント作成時に**繰り返し**を選べるように: 毎日／平日（月〜金）／毎週／毎月
- **繰り返し終了日**を指定（空欄なら約3か月先まで。1シリーズ最大366回）
- 各回を実体化して同じ `series_id` でまとめる（各回ごとにRSVP・出欠制ルームの当日判定が
  そのまま機能する。朝会を「平日・毎朝」で一度に仕込める）
- カレンダー一覧・本日の予定に **🔁** と繰り返しラベル（毎日／平日／毎週○／毎月○日）を表示
- **イベント削除**を新設（`DELETE /api/events/{id}?scope=one|series`）。繰り返しは
  「この回だけ」「繰り返し全体」を選択。削除できるのは作成者・サイト管理者・
  （チーム予定なら）そのチームのリーダーのみで、権限があるイベントにだけ削除ボタンを表示

## イベント詳細モーダル・編集（2026-06-13）

- カレンダーの日付に**マウスオーバーすると概要**（その日の予定の時刻・タイトル）が出る
- 予定を**クリックすると詳細モーダル**を表示（日時・繰り返し・公開範囲・連携ルーム・
  参加予定・作成者）。1日に複数あるときは日別一覧モーダルから選択
- 詳細の「**編集**」で日時・タイトル・公開範囲・ルーム連携を変更（`PUT /api/events/{id}`）。
  繰り返しは「この回だけ／繰り返し全体（日付以外）」を選択
- 編集ボタンは権限のある人（作成者・サイト管理者・チームリーダー）にのみ表示。
  詳細から入室・削除もできる
- カレンダー一覧・本日の予定のタイトルはクリックで詳細を開ける

## 開発者モード（DBブラウザ）（2026-06-13）

- システム管理者がメインサイトにログインしたときだけ、ヘッダーに緑色の **🛠 開発者** ボタンを表示
  （システム管理と並ぶ特別な見た目）。APIも「システム管理者かつメインサイト」で二重チェック
- 開発者画面に**テーブル一覧**（行数・列数つき）。テーブルを選ぶとビューア（広いモーダル）が開く
- ビューアでは **カラム×演算子×値** の条件(AND)で絞り込み、列見出しクリックでソート、表示件数を指定
- **安全設計**: 生SQLは受け取らない。テーブル名・カラム名はSQLAlchemyのメタデータと照合し、
  SELECTはCoreの式ビルダ＋パラメータバインドで組み立てる（SQLインジェクション不可）。
  参照のみ・LIMIT強制（最大1000行）・`password_hash` 等の機微カラムは `***` でマスク
- API: `GET /api/dev/tables`、`POST /api/dev/query`

## セキュリティ: トークンの本人性バインド + APIキャッシュ抑止（2026-06-13）

- **別端末で他人として勝手にログインされる事故の対策**。原因は、JWTが連番のユーザーID
  しか持たず、Cloud Runの揮発DB(再デプロイ/インスタンスごとにIDが振り直される)と
  組み合わさると「IDだけ一致する別人」のトークンが通ってしまうこと
- ユーザー行ごとに乱数の **`token_version`** を持たせ、JWTに含めて検証。DBが作り直されて
  IDが別人に割り当たっても、`token_version` が一致しないトークンは **401(再ログイン要求)** で弾く
- API応答に **`Cache-Control: no-store, private`** と **`Vary: Authorization`** を付与し、
  共有キャッシュ経由でユーザー固有データが他者に渡る経路を塞いだ
- ※恒久対応(正規ユーザーが再デプロイで落ちない)には**DBの永続化**が必要（[TODO.md](TODO.md)）

## テーマをユーザーに紐づけ + モバイルのヘッダー崩れ修正（2026-06-13）

- **テーマがユーザーに紐づくように**修正(これまではブラウザ単位の `localStorage` のみで、
  同じ端末で別ユーザーがログインしても前のユーザーのテーマが残っていた)。
  `users.theme` に保存し、`/api/me`・ログイン応答で返して**ログインしたユーザーのテーマを適用**
- **ログイン画面は必ずスタンダード**で表示(未ログイン時はテーマを適用しない。
  ログアウト時に `localStorage` のテーマキャッシュも消す)。localStorageは
  ログイン中のちらつき防止キャッシュとしてのみ使用
- **スマホでヘッダーが崩れる問題を修正**: システム管理者だと
  「管理画面/システム管理/開発者」のボタンが多く、狭い画面で溢れていた。
  ヘッダーを折り返し可能にし、ユーザー情報を2行目へ・ボタンを小さく・
  ローマ字サブタイトルを省略する応答的レイアウトに

## デモデータを開発者ページへ移動 + 動作確認しやすいデータを追加（2026-06-13）

- デモデータの生成・削除を **システム管理画面 → 開発者ページ(🛠 開発者)** へ移動。
  APIも `POST/DELETE /api/dev/demo-data`(システム管理者かつメインサイト)に変更
- 動作確認しやすいデータを追加: **administratorがリーダーの「デモ_管理者チーム」**、
  そのチームに紐づく通常ルーム「デモ_管理者ルーム」、**本日のミーティング**(ルーム連携・
  administratorは参加済み)、来週の予定、チーム掲示板。
  ログイン中のadministratorのダッシュボードからチーム・ルーム・本日の予定をすぐ試せる
- デモ削除時に、デモ外ユーザー(administrator)のチーム所属・RSVPも掃除するよう修正

## 管理画面をチームリーダーに開放（スコープ表示）+ お知らせのチーム限定（2026-06-13）

- **チームリーダーも管理画面を開ける**ように。サイト管理者でないリーダーは
  「**管理対象のみ**」に絞り込んで表示:
  - ユーザー一覧: 自分が率いるチームのメンバーのみ（閲覧のみ・操作ボタンなし）
  - レポート・ログ: 管理対象ユーザーのみに集計（通報も自チームのみ。監査ログは非表示）
  - チーム: 自分が率いるチームのみ（詳細・編集は可、作成/削除はサイト管理者のみ）
  - サイト設定タブ: 非表示（サイト管理者のみ）
- **お知らせにチーム限定/サイト全体のフラグ**（`Announcement.team_id`）。
  サイト全体はサイト管理者のみ、チーム限定はそのチームのリーダーが投稿可能。
  表示は所属チームのメンバーにだけ届く。一覧・ダッシュボードにチーム名タグを表示
- ヘッダーの「**管理画面**」をシステム管理・開発者と並ぶ**ピル型の強調ボタン**(🗂)に
- API: 関連エンドポイント(`/api/admin/users`・`report`・`teams`・`announcements`)を
  `admin_or_leader_user` でリーダー対応にし、サイト管理者でない場合は管理対象にスコープ。
  サイト設定・監査ログはサイト管理者専用のまま

## 出欠制ルームの入室ボタン無効化（2026-06-13）

- 出欠制ルームで本日「参加」を選んでいない(不参加・未回答)場合、**入室できないように
  ボタンを無効化**するよう修正。これまでは「入室する」→「マッチング開始」を押すと、
  サーバー側のゲートで弾かれるが**モーダルが閉じて何も表示されない**状態だった
- 本日の予定バナーの「入室する」、ルーム入室モーダルの「マッチング開始/役割別参加」を
  対象に無効化し、理由(本日の予定から「参加」を選ぶ)を明示。「参加」を選ぶと有効化される

## お知らせ機能の拡張（既読・詳細モーダル・変数リンク）（2026-06-14）

- **ダッシュボードは省略表示**(本文2行)。お知らせのタイトルをクリックで**詳細モーダル**に全文表示
- 詳細モーダルに**前へ/次へ**の矢印ボタン。前後のお知らせへ移動できる
- **既読管理**(`AnnouncementRead`)。詳細を開くと既読になり、**未読には「新着」バッジ**を表示
- **投稿権限者(サイト管理者/そのチームのリーダー)は詳細で送信対象人数・既読人数**を確認可能
  (`GET /api/announcements/{id}/stats`)
- **本文の変数**: `{roomN}` と書くと**ルーム番号Nへのリンク**になり、押すとルーム詳細モーダルが開く
  - ルームに**サイト内一意の連番(room_no)**を自動付与(`Room.room_no`)
  - 投稿フォームの「本文」横の **? ヒント**で、使える変数と現在のルーム番号一覧を表示
- デモデータ生成にローカル日付を渡せるようにし(`?date=`)、本日のデモイベントの日付ずれ(時差)を解消
- お知らせ詳細モーダルのデザイン刷新: **前へ/次へ/閉じるを右上のアイコン**(‹ › ✕)に集約。
  既読人数は**本文の上部**にハイライト表示
- お知らせ詳細モーダルの**表示位置を固定**(上揃え+本文だけスクロール)。本文の分量が違っても
  「次へ」ボタンの位置がずれず、連続クリックで読み進められる
- 既読人数に加え**未読人数も表示**。既読数/未読数を**押すと既読・未読のユーザー一覧**を確認できる
  (`GET /api/announcements/{id}/readers`、投稿権限者のみ)

## ルームID表示・出欠制の導線改善・デモデータ修正（2026-06-14）

- ルーム詳細モーダルに**ルームID(room_no)**を表示(お知らせ変数 `{roomN}` の番号が分かる)
- 出欠制ルームで入室できないときのメッセージを改善: 本日の予定にそのルームのイベントがあれば
  **「▶ 本日の予定で『参加』を選ぶ」リンク**を表示し、押すとバナーへスクロール&ハイライト。
  自分の本日の予定に無い場合(対象外)はその旨を明示
- デモデータ修正:
  - デモ作成のルームに**room_noが採番されていなかった**不具合を修正(`{roomN}`が機能するように)
  - 「デモ_管理者ルーム」を**出欠制**に変更。administratorは管理者チームのメンバーなので
    本日のミーティングが「本日の予定」に出る → 参加/不参加の切替で入室ボタンの活性が変わるのを
    そのまま確認できる(以前は通常ルームのため不参加でも入室できて紛らわしかった)

## 品質管理

- 自動結合テスト **302項目**（`tests/test_flow.py`）。認証・マッチング・シグナリング・
  マルチテナント・チーム・ルーム・T&S・各種設定・デモデータ・マイページ・
  イベント時刻/本日の予定/RSVP/出欠制マッチング・繰り返し・イベント編集・開発者モード・
  トークン本人性バインド/キャッシュ抑止・ユーザー別テーマ・リーダーの管理画面スコープ・
  お知らせのチーム限定/既読/統計/ルーム番号変数の動作を毎回検証
- 本番運用前の課題は [TODO.md](TODO.md) に記録（TURNサーバー・DB永続化・SECRET_KEY・レート制限など）
